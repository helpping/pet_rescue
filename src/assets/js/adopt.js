$(function(){
  /*$(".carousel-adopt-tab li").on("click",function(){
      $(this).siblings().removeClass('active');
      $(this).addClass('active');
  });
  $(".filter1 li span").on('click',function(){
    if($(this).attr('class')=='selected'){
      $(this).removeClass('selected');
    }else{
      $(this).parent().siblings().children().removeClass('selected');
      $(this).addClass('selected');
    }
  });
  $(".filter2 li span").on('click',function(){
    if($(this).attr('class')=='selected'){
      $(this).removeClass('selected');
    }else{
      $(this).parent().siblings().children().removeClass('selected');
      $(this).addClass('selected');
    }
  });*/

  /*$('.adopt-bottom').each(function(){
    var text=$(this).html();
    var text2=text.substring(0,30)+'.....';
    $(this).html(text.length > 30 ? text2:text);
    console.log(text);
    console.log(text2);
  });*/

});
